apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ddclient.fullname" . }}-test-connection"
  labels:
    {{- include "ddclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ddclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
